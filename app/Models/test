$results = App\Models\Product::search($searchTerm, function($engine, $query, $options) {
    // Filtres stricts pour vendor et type
    $options['filter_by'] = "vendor:= `Hermès` && type:= `Eau de Toilette Recharge`";

    // Ne rechercher que sur name et variation (puisque vendor et type sont filtrés)
    $options['query_by'] = 'name,variation';

    // Configurer la tolérance
    $options['num_typos'] = '1,2'; // 1 faute pour name, 2 pour variation
    $options['prefix'] = 'false,false'; // Pas de prefix

    return $options;
})->get();
